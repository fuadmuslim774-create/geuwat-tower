# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Real-Time Completion Timer for In-Progress Users
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: Scope the property to users with journeyStartedAt not null, journeyCompletedAt null, and time_sec null
  - Test that for any user where isBugCondition(user) returns true, the leaderboard displays a running timer showing elapsed time in HH:MM:SS format (not "--:--:--")
  - Generate test cases with various journeyStartedAt timestamps (e.g., 1 minute ago, 5 minutes ago, 1 hour ago)
  - Assert that displayedTime equals formatHMS(elapsedTime) where elapsedTime = currentTime - journeyStartedAt
  - Assert that displayedTime is NOT "--:--:--"
  - Assert that timer increments over time (check at T=0 and T=5 seconds)
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found (e.g., "User with journeyStartedAt=1704067200000 shows '--:--:--' instead of elapsed time '00:05:30'")
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 2.1, 2.2, 2.3_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Final Completion Time and Not-Started Users
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs:
    - Users with journeyCompletedAt not null and time_sec not null (completed users)
    - Users with journeyStartedAt null (not started users)
  - Write property-based tests capturing observed behavior patterns:
    - **Test 2.1**: For all users where journeyCompletedAt IS NOT NULL and time_sec IS NOT NULL, verify displayed time equals formatHMS(time_sec) and does NOT change over time
    - **Test 2.2**: For all users where journeyStartedAt IS NULL, verify displayed time equals "--:--:--"
    - **Test 2.3**: For all leaderboard queries, verify ranking order is sorted by rank_stage_id (descending) then time_sec (ascending)
    - **Test 2.4**: For all stage completions, verify bestTimeSeconds and lastTimeSeconds are saved correctly in local storage
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4_

- [ ] 3. Fix for completion timer not starting

  - [x] 3.1 Phase 1: Create server-side API endpoint for leaderboard sync
    - Create new file `app/api/leaderboard/sync/route.ts`
    - Implement POST endpoint that accepts payload: { userId, rankStageId, timeSec }
    - Add authentication check using Supabase auth to ensure user can only update their own entry
    - Implement upsert logic using Supabase service role client (insert if not exists, update if exists)
    - Validate input data (userId must match authenticated user, rankStageId must be valid StageId, timeSec must be non-negative or null)
    - Return appropriate HTTP status codes (200 for success, 400 for validation errors, 401 for unauthorized, 500 for server errors)
    - Add error logging for debugging
    - _Bug_Condition: isBugCondition(input) where input.journeyStartedAt IS NOT NULL AND input.journeyCompletedAt IS NULL AND input.timeSec IS NULL_
    - _Expected_Behavior: API endpoint successfully creates/updates leaderboard entry with calculated timeSec_
    - _Preservation: Existing leaderboard entries for completed users remain unchanged_
    - _Requirements: 2.1, 2.4_

  - [x] 3.2 Phase 2: Modify client-side progress tracking to call API
    - Modify `lib/progress.ts` function `updateOnStageComplete`
    - After writeProgress call, add API call to `/api/leaderboard/sync`
    - Calculate timeSec using `getJourneyCompletionTimeSeconds()` function
    - Get rankStageId using `getRankStageId()` function
    - Construct payload: { userId: profile.id, rankStageId, timeSec }
    - Make POST request to `/api/leaderboard/sync` with payload
    - Add error handling: log error but don't block user flow (user can continue playing even if sync fails)
    - Consider adding retry logic (optional, can be implemented later)
    - _Bug_Condition: isBugCondition(input) where input.journeyStartedAt IS NOT NULL AND input.journeyCompletedAt IS NULL AND input.timeSec IS NULL_
    - _Expected_Behavior: Leaderboard entry is synced to database when user completes a stage_
    - _Preservation: Stage-level time tracking (bestTimeSeconds, lastTimeSeconds) continues to work as before_
    - _Requirements: 2.1, 2.4, 3.4_

  - [x] 3.3 Phase 3: Enhance leaderboard display logic with real-time calculation
    - Modify `lib/supabase/leaderboard.ts` function `fetchRanksBase`
    - For each leaderboard entry, check if time_sec is null
    - If time_sec is null, fetch journeyStartedAt from local storage for current user
    - If journeyStartedAt is valid (not null), calculate elapsed time: (currentTime - journeyStartedAt) / 1000
    - Format elapsed time using formatHMS function
    - Update return mapping to use calculated time instead of "--:--:--" for in-progress users
    - Keep "--:--:--" only for users who haven't started journey (journeyStartedAt is null)
    - _Bug_Condition: isBugCondition(input) where input.journeyStartedAt IS NOT NULL AND input.journeyCompletedAt IS NULL AND input.timeSec IS NULL_
    - _Expected_Behavior: Leaderboard displays running timer for in-progress users based on journeyStartedAt_
    - _Preservation: Completed users continue to show final static completion time from database_
    - _Requirements: 2.2, 2.3, 3.1, 3.2_

  - [x] 3.4 Phase 4: Add client-side timer updates for real-time display
    - Modify `app/ranks/page.tsx` to add useEffect hook for timer updates
    - Add state variable to track current time for timer recalculation
    - Set up interval (setInterval) to update current time every 1 second
    - Cleanup interval on component unmount (return cleanup function from useEffect)
    - For current user row, calculate and display running timer if user is in progress
    - Display static time if user has completed journey
    - Repeat same changes for `app/generation-ranks/page.tsx`
    - _Bug_Condition: isBugCondition(input) where input.journeyStartedAt IS NOT NULL AND input.journeyCompletedAt IS NULL AND input.timeSec IS NULL_
    - _Expected_Behavior: Timer increments every second on leaderboard page for in-progress users_
    - _Preservation: Page refresh preserves correct elapsed time (not reset to zero)_
    - _Requirements: 2.3, 3.1_

  - [x] 3.5 Phase 5 (Optional): Database schema enhancement for journey timestamps
    - Modify `supabase/schema.sql` to add columns to leaderboard_entries table
    - Add column: journey_started_at timestamptz null
    - Add column: journey_completed_at timestamptz null
    - Update view v_global_ranks_ordered to include new fields
    - Modify API endpoint `/api/leaderboard/sync` to accept and store journey timestamps
    - Update client-side sync call to include journeyStartedAt and journeyCompletedAt in payload
    - This enables future consistency and server-side calculation without relying on local storage
    - _Bug_Condition: isBugCondition(input) where input.journeyStartedAt IS NOT NULL AND input.journeyCompletedAt IS NULL AND input.timeSec IS NULL_
    - _Expected_Behavior: Journey timestamps are persisted in database for future reference_
    - _Preservation: Existing leaderboard functionality remains unchanged_
    - _Requirements: 2.1, 2.4_

  - [x] 3.6 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Real-Time Completion Timer for In-Progress Users
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - Verify that for users with journeyStartedAt not null, journeyCompletedAt null, and time_sec null, the leaderboard displays running timer
    - Verify timer shows correct elapsed time in HH:MM:SS format
    - Verify timer increments over time (not static)
    - _Requirements: 2.1, 2.2, 2.3_

  - [x] 3.7 Verify preservation tests still pass
    - **Property 2: Preservation** - Final Completion Time and Not-Started Users
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Verify completed users still show final static completion time (unchanged)
    - Verify not-started users still show "--:--:--"
    - Verify ranking order is preserved (rank_stage_id desc, time_sec asc)
    - Verify stage-level time tracking still works correctly
    - Confirm all tests still pass after fix (no regressions)

- [x] 4. Checkpoint - Ensure all tests pass
  - Run all property-based tests (bug condition + preservation)
  - Run unit tests for helper functions (getJourneyCompletionTimeSeconds, formatHMS, getRankStageId)
  - Run integration tests for full user flow (login → complete stage → check leaderboard)
  - Verify timer displays correctly on both Global Ranks and Generation Ranks pages
  - Verify API endpoint works correctly with authentication
  - Verify no regressions in existing functionality
  - If any issues arise, investigate and ask user for guidance
  - Document any edge cases or limitations discovered during testing
